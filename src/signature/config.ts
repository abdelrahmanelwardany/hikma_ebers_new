// Signature features — each can be toggled independently for testing.
export const signatureConfig = {
  customCursor: true, // Feature 1: cursor morph on interactive hover
  loadingMark: true, // Feature 2: brief animated mark on initial load
  footerWatermark: true, // Feature 3: low-contrast footer micro-detail
  consoleEgg: true, // Feature 4: styled console message on load
  hiddenReveal: true, // Feature 5: gated reveal via footer mark / logo clicks
};
